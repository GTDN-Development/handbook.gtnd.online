---
title: "Container"
description: "Provides unified content width constraints across the app with fluid, responsive behavior."
highlight: true
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Container } from "@/registry/v2/ui/container";


## Usage

```tsx
import { Container } from "@/components/ui/container";
```

```tsx
<Container>
  Wrapped content
</Container>
```

## Installation

<Steps>
    <Step>
        **Install dependencies**

        ```bash
        npm install @radix-ui/react-slot class-variance-authority
        ```
    </Step>
    <Step>
        **Create the component file**

        ```bash
        touch src/components/ui/container.tsx
        ```
    </Step>
    <Step>
        **Copy & paste the source code**

        <include cwd lang="tsx" meta='title="components/ui/container.tsx"'>
          src/registry/v2/ui/container.tsx
        </include>

        Update the import paths to match your project setup.
    </Step>
</Steps>

## API Reference

<TypeTable
    type={{
    size: {
        type: '"sm" | "md" | "lg" | "xl" | "2xl" | "prose" | "default"',
        description: 'Width variants of the container. "sm" = 640px, "md" = 768px, "lg" = 1024px, "xl" = 1280px, "2xl" = 1536px, "prose" = 65ch, "default" = 1536px (2xl)',
    },
    asChild: {
        type: 'boolean',
        description: 'Render as a child element using Radix UI Slot',
    },
    className: {
        type: 'string',
        description: 'Additional CSS classes',
    },
    }}
/>
