---
title: "Cn"
description: "A utility for constructing className strings conditionally. Combines clsx for handling conditional class names with twMerge for intelligently merging Tailwind CSS classes and resolving conflicts"
highlight: true
---


import { Step, Steps } from "fumadocs-ui/components/steps";

## Usage
```tsx
import { cn } from "@/lib/utils";

function YourFunction({ className }) {
  return (
    <div
      className={cn(
        "flex items-start gap-2",
        className
      )}
    >
      Lorem ipsum dolor
    </div>
  );
}
```

## Installation

<Steps>
<Step>
**Create your lib/utils.ts file**

```bash
touch src/lib/utils.ts
```
</Step>

<Step>
**Install dependencies**

```bash
npm install clsx tailwind-merge
```
</Step>

<Step>
**Copy & paste the source code to your lib/utils.ts file**

```tsx title="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</Step>
</Steps>
